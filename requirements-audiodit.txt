# Optional: LongCat-AudioDiT as --tts_provider longcat_audiodit (text -> wav -> avatar).
# Install on top of the main ARACHNE-X environment. May require transformers>=5.3; check for conflicts with core requirements.txt.
#
# Upstream: https://github.com/meituan-longcat/LongCat-AudioDiT
# Weights: https://huggingface.co/meituan-longcat/LongCat-AudioDiT-1B
#
transformers>=5.3.0
torchaudio>=2.0.0
safetensors>=0.4.0
einops>=0.8.0
soundfile>=0.12.0
librosa>=0.10.0
