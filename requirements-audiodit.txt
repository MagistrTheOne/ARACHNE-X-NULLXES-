# Optional: AudioDiT as --tts_provider audiodit (legacy CLI alias: longcat_audiodit).# NOT compatible in the same venv as core ARACHNE-X (transformers>=5.3 vs 4.41.0).
# Use a dedicated environment or container layer.
#
transformers>=5.3.0
torchaudio>=2.0.0
safetensors>=0.4.0
einops>=0.8.0
soundfile==0.13.1
librosa==0.11.0
