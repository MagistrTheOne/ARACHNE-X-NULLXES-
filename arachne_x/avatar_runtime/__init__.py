"""Internal mixins for the ARACHNE production avatar pipeline.

These modules are behavior-preserving extractions from
``arachne_x.pipeline_arachne_x_video_avatar``. They are not standalone runtime
entrypoints; the public import contract remains the main pipeline module.
"""
