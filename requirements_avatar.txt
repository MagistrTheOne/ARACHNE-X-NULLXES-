# Avatar / GPU worker production extras (install AFTER requirements.txt + flash-attn).
#
# Verified imports: arachne_x/pipeline_arachne_x_video_avatar.py, inference_audio.py,
# avatar_serving.py, loader.py — no sklearn / onnx / demo WebRTC stack in prod path.
#
-r requirements.txt

# librosa resampling backend (used transitively by audio load paths)
soxr==0.5.0.post1
